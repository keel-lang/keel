[package]
name = "keel-catalog"
version = "0.2.1"
edition = "2024"
license = "MIT"
description = "Keel stdlib namespace catalog: neutral method descriptors and capability metadata."
publish = false

[dependencies]
