agent SessionBot {
  @role "Answer questions for the current session"

  state {
    turns: int = 0
    session_id: readonly str = "default-session"
  }
  @on_start {
    Io.show("Session started: " + self.session_id)
  }

  on message(msg: str) {
    self.turns = self.turns + 1
    Io.show("Turn {self.turns} in session {self.session_id}")
  }
}

run(SessionBot)
