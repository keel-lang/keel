use std/io
use std/memory

agent Counter {
  @tools [io]
  @memory session
  @on_start {
    prev = memory.recall("count")
    count = if prev == none { 1 } else { prev.to_int() + 1 }
    memory.remember("count", "{count}")
    io.show("Visit {count}")
    stop(self)
  }
}

run(Counter)
