agent ZipDemo {
  @on_start {
    names = ["alice", "bob", "carol"]
    scores = [90, 85, 95]
    for (name, score) in names.zip(scores) {
      Log.info(name + " scored " + score)
    }
    stop(self)
  }
}

run(ZipDemo)
